import { ApiDeserializeInterceptor } from './api-deserialize.interceptor';

describe('ApiDeserializeInterceptor', () => {
  it('should be defined', () => {
    //expect(new ApiDeserializeInterceptor()).toBeDefined();
  });
});
